## What I learned This Week

- The script tag holds all the JavaScript code
- Put it at the bottom of the HTML file, before body ends, so it loads after the page content
- When you click the button, the add task function runs
- The function gets text from the input and adds it to the page
    
- `innerHTML` - changes the content inside an element
- `appendChild()` - adds a new element to the page
- `style.display` - shows or hides an element
- `className` - changes the CSS class of an element

- We can use external files to add JavaScript to our HTML. This makes it easier to read. I added a `todo.js` file and linked it to my HTML file. I can write all my JavaScript in that file and it will work on the page. It makes it super clean. 
- We can set the id in two ways as we saw. Either in the HTML element itself or by using JavaScript to set it. 
- document.querySelector() is a powerful way to find elements on the page. It can find by id, class, or even more complex selectors. There must be some h1 on the page for it to work.

- element.style.display = 'none' → hides the element
- element.style.display = 'block' → shows the element



## Challenges I faced

- Remembering that JavaScript needs to find elements by their id. 
- Understanding how `appendChild()` actually makes things appear on the page. 

## AI Usage

- Used AI to build the to-do app. It shows how JavaScript talks to HTML and makes things happen when you click buttons.

## Questions

- How does the page remember tasks after I refresh?
- What else can I make clickable besides buttons?

## Review Questions From Class

- You wrote const btn = document.querySelector('button') but clicking the button does nothing. What line of code is missing? You need to add an event listener to the button to tell it what to do when it's clicked. For example: `btn.addEventListener('click', function() { /* code to run on click */ });`

- document.querySelector('myButton') returns null, but <button id="myButton"> is right there in the HTML. What's wrong? Remember that querySelector needs a selector, so it should be document.querySelector('#myButton') to find the element by its id.

- You changed a heading's color in the Console and it worked. After refreshing the page, it's gone. Why? Because the change you made in the Console is temporary and only exists in that session. When you refresh the page, it reloads the original HTML and CSS, so any changes made in the Console will be lost.

- You see onclick="doSomething()" in someone's HTML. What's a better way to set up that event in JavaScript, and why?  A better way is to use addEventListener in your JavaScript file, like this: `element.addEventListener('click', doSomething);`. This keeps your HTML clean and separates your structure (HTML) from your behavior (JavaScript), which is a good practice for maintainability and readability.