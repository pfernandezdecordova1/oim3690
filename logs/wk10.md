## What I learned This Week
- All languages should have variables (let/const), data types, operators, templates literals..
- We will talk about functions today. Functions are reusable pieces of code that perform a specific task. They can take in parameters and return a value.
- For tripple = sign rememeber it checks both type and value. So 10 === '10' is false because they are different types, even though they have the same value. (due to the quotes, not exactly the same) 
- % is modular operator, remainder of division. Asterisk is to the power of. 
- check the string methods in js. They are a lot, we will not cover all.
- Comparison operators: >, <, >=, <=, ==, ===, !=, !==
- Logical operators: && (and), || (or), ! (not)

# Functions
 Its a reusable block of code that performs a task. It only does one task. By adding parameters you make your function more flexible. 

ex: function greet(name). Where is name going? name is the parameter, it is moved on as an argument when we call the function. console lo then prints the function, ex: greet('Alice') will print 'Hello, Alice!'.

- Arrow functions: Its just a format. same thing but it unifies it. It does the same thing but it is easyer to read.
- You can pass any function to addEventListener. It will run when the event happens.
# Conditions 
- if we have if else boxex it will check sequentially, In order. It will end the block if the condition is true and it will not check the rest of the conditions. 

# Thursday class: We went over Arrays and Loops

## Challenges I faced
- Remember return is different that console log because return gives back a value that can be used later, while console log just prints something to the console and does not affect the flow of the program.
- Comparing return vs no return in a function. If a function does not have a return statement, it will return undefined by default. This means that if you try to use the value of a function that does not return anything, you will get undefined.